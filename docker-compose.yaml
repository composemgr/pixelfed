# nginx proxy address - http://172.17.0.1:8080

x-logging: &default-logging
  driver: json-file
  options:
    max-size: "5m"
    max-file: "1"

services:
  pixelfed:
    image: pixelfed/pixelfed:latest
    container_name: pixelfed-app
    restart: unless-stopped
    logging: *default-logging
    networks:
      - pixelfed
    ports:
      - "8080:80"
    environment:
      TZ: ${TZ:-America/New_York}
    volumes:
      - ./rootfs/data/pixelfed:/data
      - ./rootfs/config/pixelfed:/config
    depends_on:
      - pixelfed-db
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80"]
      interval: 30s
      timeout: 10s
      retries: 3

  pixelfed-db:
    image: mariadb:11
    container_name: pixelfed-db
    restart: unless-stopped
    logging: *default-logging
    networks:
      - pixelfed
    environment:
      TZ: ${TZ:-America/New_York}
      MARIADB_DATABASE: ${DB_CREATE_DATABASE_NAME:-pixelfed}
      MARIADB_USER: ${DB_USER_NAME:-pixelfed}
      MARIADB_PASSWORD: ${DB_USER_PASS:-changeme_db_password}
      MARIADB_ROOT_PASSWORD: ${DB_ADMIN_PASS:-changeme_root_password}
    volumes:
      - ./rootfs/db/mariadb/pixelfed:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  pixelfed:
    external: false
